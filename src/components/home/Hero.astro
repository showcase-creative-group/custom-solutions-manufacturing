---
// src/components/home/Hero.astro
---

<section class="hero-section">
  <!-- Cloudflare R2 Video Background -->
  <video class="hero-video" autoplay loop muted playsinline>
    <source src="https://pub-6b61e64b6d9145a3b0f5e845de93fbb9.r2.dev/Home/csm_hero_manufacturing_portfolio_for_web.mp4" type="video/mp4" />
  </video>
  
  <!-- Lighter Gradient Overlay -->
  <div class="hero-overlay"></div>

  <div class="hero-container">
    <h1>Turn-Key, Specialized<br>Manufacturing</h1>
    <p>Discover precision machining and custom welding to serve your needs.</p>
  </div>
</section>

<style>
  .hero-section {
    background-color: var(--brand-black); 
    color: #ffffff;
    
    width: 100%;
    min-height: 100vh;
    box-sizing: border-box;
    
    display: flex;
    align-items: center;
    
    padding: 120px 2rem 5rem 2rem;
    position: relative;
    overflow: hidden;
  }

  .hero-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.6));
    z-index: 2;
  }

  .hero-container {
    max-width: 1200px;
    margin: 0 auto;
    width: 100%;
    position: relative;
    z-index: 3;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
  }

  h1 {
    font-family: var(--font-saira);
    font-size: clamp(2.5rem, 5vw, 4.5rem); 
    font-weight: 800;
    text-transform: uppercase;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    max-width: 800px;
    color: #ffffff;
    letter-spacing: -0.5px;
  }

  /* Refactored paragraph text */
  p {
    font-size: clamp(1.15rem, 2vw, 1.35rem); /* Slightly larger */
    font-weight: 500; /* Heavier weight for video legibility */
    max-width: 600px;
    color: rgba(255, 255, 255, 0.9); /* High contrast, but softer than the h1 */
    margin: 0;
    line-height: 1.6;
    letter-spacing: 0.3px; /* Gives the letters room to breathe */
  }

  @media (max-width: 768px) {
    .hero-section {
      padding-top: 100px;
      min-height: 80vh;
    }
  }
</style>